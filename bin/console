#!/usr/bin/env ruby
# frozen_string_literal: true

require "irb"
require "italian/ruby"

##
# Volendo si potrebbe eseguire Italian Ruby anche direttamente da IRB,
# basta decommentare questo pezzettino di codice.
#
# Il problema è che non sono ancora riuscito a capire come modificare
# l'evidenziazione sintassi di IRB, quindi il codice ItalianRuby
# non è molto esteticamente bello da vedere perché IRB non lo capisce.
#
# module IRB
#   class Context
#     alias :original_evaluate :evaluate
# 
#     def evaluate(line, lineno, exception: nil)
#       translated_line = Italian::Ruby::Traduttore.traduci nil, line
#       original_evaluate translated_line, lineno, exception: exception
#     end
#   end
# end

##
# Avvio IRB.
ARGV.clear
IRB.start __FILE__
